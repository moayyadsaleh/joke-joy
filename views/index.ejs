<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <title>JokeJoy</title>
</head>
<body>
    <div class="container">
        <h1>Get ready to chuckle, giggle, and burst into laughter!</h1>

        <form action="/" method="post">
            <button type="submit">Get a Joke!</button>
        </form>
            <%if (locals.data) {%>
                <h2>
                  <%=data.joke %>
                </h2>
              <%}%>
                <%if (locals.error) {%>
                  <div id="tag-error" class="tag-error">
                    <%=error%>
                  </div>
                  <%}%>
        </div>
    </div>
</body>
</html>